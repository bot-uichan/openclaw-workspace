# dlsite-play-cookie-helper

`dlsite-tui` 向けの optional 認証ヘルパー（Playwright利用）。

## setup
```bash
cd apps/dlsite-play-cookie-helper
npm i
npm link
```

## command
```bash
dlsite-tui-cookie-helper
```

標準出力に `{ "cookies": [...] }` を返します。
