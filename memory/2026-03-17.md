# 2026-03-17

- あかずさん依頼で OpenClaw workspace から Todoist を読む連携を追加。
- `.env.todoist` に個人トークンを保存（gitignore対象）。
- `scripts/todoist-read.mjs` を追加し、Todoist API v1 で `today` / `all` / `projects` を取得可能にした。
- `notes/todoist.md` に使い方メモを追加。
- `/skill_creator todoistでタスクを確認する` に対応するため、`skills/public/todoist-check` と `skills/dist/todoist-check.skill` を追加。
- `todoist-check` skill を `skills/public` から `skills/` 直下へ移動し、wrapper shell を削除して OpenClaw の探索形に修正。
- `todoist-check` skill に、タスク追加時は先にプロジェクト一覧を確認して最適な追加先を選ぶフローを追記。
